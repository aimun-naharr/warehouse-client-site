import React from 'react';
import { Spinner } from 'react-bootstrap';

const AddSpinner = () => {
    return (
        <div>
            <Spinner animation="border justify-content-center mx-auto container align-items-center d-flex" variant="primary" />
        </div>
    );
};

export default AddSpinner;